/**
 * 이 패키지의 아이콘 타입 단일 출처.
 *
 * 컴포넌트는 `@phosphor-icons/react` 를 직접 import 하지 않고 여기서 가져온다.
 * 아이콘 세트를 하나로 강제하는 결정(2026-09)이 이 파일 하나에 모여 있어야
 * 나중에 바뀔 때 고칠 자리가 하나다.
 */
export type { Icon, IconProps, IconWeight } from "@phosphor-icons/react";
